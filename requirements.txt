alembic>=1.13
fastapi>=0.110
pydantic>=2.6
sqlalchemy>=2.0
uvicorn[standard]>=0.29

# Packet 10: RESEARCH_PROVIDER=tavily/brave need this to make live HTTP calls.
# Not needed for RESEARCH_PROVIDER=fixture, but installed unconditionally so a
# live provider is one env var away rather than a separate install step.
httpx>=0.27

# Packet 11: LLM_PROVIDER=anthropic needs this. Not needed for
# LLM_PROVIDER=fixture, but installed unconditionally like httpx above.
# >=1.2 for native structured outputs (client.messages.parse / output_format).
anthropic>=1.2

# Packet 12: The Fishbowl's server-rendered pages (app/web/pages.py). The
# lightest templating option that fits the existing FastAPI app — chosen
# over a React/Vite toolchain per Packet 12's own "prefer simplicity".
jinja2>=3.1
