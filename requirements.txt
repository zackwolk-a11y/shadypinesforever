alembic>=1.13
fastapi>=0.110
pydantic>=2.6
sqlalchemy>=2.0
uvicorn[standard]>=0.29

# Packet 10: RESEARCH_PROVIDER=tavily/brave need this to make live HTTP calls.
# Not needed for RESEARCH_PROVIDER=fixture, but installed unconditionally so a
# live provider is one env var away rather than a separate install step.
httpx>=0.27

# Optional — only needed for a live LLM provider, never for the fixture path.
# anthropic>=0.40      # LLM_PROVIDER=anthropic
